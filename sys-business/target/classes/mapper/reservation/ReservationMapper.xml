<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cuit.business.reservation.mapper.ReservationMapper">
    <resultMap id="ReservationMap" type="com.cuit.business.reservation.domain.Reservation">
        <result property="id" column="id"/>
        <result property="eid"    column="eid"    />
        <result property="hid"    column="hid"    />
        <result property="startTime"    column="start_time"    />
        <result property="endTime"    column="end_time"    />
        <result property="delFlag" column="del_flag"/>
        <result property="createId"    column="create_id"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateId"    column="update_id"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <select id="selectBySId" resultType="com.cuit.business.reservation.domain.Reservation">
        select id,eid,hid,start_time,end_time,del_flag,create_id,create_time,update_id,update_time
        from reservation
        <where>
            del_flag = '0'
            <if test="eid!= null and eid!= ''"> and eid = #{eid}</if>
            <if test="hid!= null and hid!= ''"> and hid = #{hid}</if>
        </where>
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cuit.system.sysRoles.mapper.SysRolesMapper">

    <resultMap id="Roles" type="com.cuit.system.sysRoles.domain.SysRoles">
        <result property="id"    column="id"    />
        <result property="roleName"    column="role_name"    />
        <result property="status"    column="status"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="createId"    column="create_id"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateId"    column="update_id"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="SysRolesList">
        select id,role_name,status,del_flag,create_id,create_time,update_id,update_time from sys_roles
    </sql>

    <update id="forbiddenById">
        update sys_roles
        set status = #{status}
        where id = #{id}
    </update>

    <select id="selectSysRolesList" resultType="com.cuit.system.sysRoles.domain.SysRoles" resultMap="Roles">
        <include refid="SysRolesList"></include>
        <where>
            del_flag = '0'
            <if test="roleName != null  and roleName != ''"> and role_name like concat('%', #{roleName}, '%')</if>
        </where>
        order by id
    </select>

</mapper>
